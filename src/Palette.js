import React, { Component } from 'react';

class Palette extends Component {
  render() {
    return (
      <div className="Palette">
        {/* navbar */}
        <div className="Palette-swatches">{/* Bunch of swatches */}</div>
        {/* footer */}
      </div>
    );
  }
}

export default Palette;
